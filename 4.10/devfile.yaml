schemaVersion: 2.2.0
metadata:
  name: fbc-4.10
  language: go
components:
  - name: image-build
    image:
      imageName: fbc:latest
      dockerfile:
        uri: Dockerfile
        buildContext: "4.10"
commands:
  - id: build-image
    apply:
      component: image-build
